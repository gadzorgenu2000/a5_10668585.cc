
#include<string>
using namespace std;
struct student
{
  string firstname;
  double score;
  int age;
  double sum;
  double average;
  char grade;
  string id;
  string gender;
};
void input(student []);
void print(student[]);
void initialise(student[]);
int main()
{
    infile<< profile[i].id>>profile[i].age>>profile[i].score>>profile[i].gender;
  
    
    return 0;
}
void initialise(student profile[])
{
    for(int i=0;i<5;i++)
    {
        profile[i].firstname;
        profile[i].id;
        profile[i].age;
        profile[i].score;
        profile[i].gender;
    }
}
void input(student profile[])
{
    for(int i=0;i<5;i++)
    {
        cout<<"Enter your firstname and last name, id,age,score,gender "<<endl;
        cin>>profile[i].firstname;
        cin>>profile[i].id>>profile[i].age>>profile[i].score>>profile[i].gender;
        if (profile[i].score>90)
        {
           cout<<"A";
        }
        else if (profile[i].score>=80 && profile[i].score<90)
        {
              cout<<"B";
        }
        else if (profile[i].score >=70 && profile[i].score<80)
        {
                cout<<"C";
        }
        else if (profile[i].score>=60 && profile[i].score<70)
        {
                cout<<"D";
        }
        else if (profile[i].score>=50 &&profile[i].score<60)
        {
               cout<<"E";
        }
        else if (profile[i].score<50)
        {
             cout<<"F";
        }
        else 
        {
             cout<<"wrong input";
        }
    }
    for(int i=0;i<5;i++)
    {
        sum+=profile[i].score;
        sum++;
    }
    for(int i=0;i<5;i++)
    {
        average =sum/5.0;
    }
}
void print(student profile[])
{
    for(int i=0; i<5; i++)
    {
        cout<<"student name "<<profile[i].firstname<<endl;
        cout<<"student id: "<<profile[i].id<<endl;
        cout<<"student age: "<<profile[i].age<<endl;
        cout<<"student grade: "<<profile[i].grade<<endl;
        cout<<"average: "<<profile[i].average<<endl;
    }
}


























